# Public site origin used for Open Graph / metadataBase.
# Use https:// in production to enable HSTS in next.config.ts
SITE_URL=http://localhost:5002

# Optional alias (same purpose)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# After DNS is ready, point profile.email at yousuf.gdls@gmail.com
# and set profile.bookingUrl to your real Calendly link.
